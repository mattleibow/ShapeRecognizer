
trigger:
- master

pool:
  vmImage: 'win-vs2017'

steps:
- task: MSBuild@1
  inputs:
    solution: 'ShapeRecognizer.sln'
    platform: 'Any CPU'
    configuration: 'Release'
    maximumCpuCount: true
    restoreNugetPackages: true
